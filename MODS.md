This fork has the following mods/patches:
- [ ] Made `Custom HTML` in Forms accept inline styles, `<script>` tags and `<iframe>` tags.
- [ ] Made `Custom HTML` in Forms accept base64 encoded images (`png`, `jpeg`, `gif`, `webp`, `bmp`, `avif`).