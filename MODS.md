This fork has the following mods/patches:
- [ ] Made `Custom HTML` in Forms accept inline styles.
- [ ] Made `Custom HTML` in Forms accept `<script>` tags.
- [x] Made `Custom HTML` in Forms accept base64 encoded images (`png`, `jpeg`, `gif`, `webp`, `bmp`, `avif`).
- [x] Made `Custom HTML` in Forms allow tags `<hr>`, `<textarea>`, 